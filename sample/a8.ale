'pragmat'title="printing permutations".
'pragmat'require="a8m1".
$ the module a8m1 provides the rule
$   'predicate'next perm+[]st[].
$ which generates the lexicographically next permutation of st,
$ and fails if elements of st are decreasing.

$ elements of V are in increasing order, this is the first
'stack'[]V[]=(/a/,/b/,/c/,/d/).
'root'(rep: put line+STDOUT+V+newline,
            (next perm+V,:rep;+).
'end'

